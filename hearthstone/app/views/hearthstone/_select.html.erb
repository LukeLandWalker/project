  <tr>
    <th>name</th>
    <th>type</th>
    <th>cost</th>
  </tr>
<% @cards.each do |card| %>
  <tr>
    <th><%= link_to card.name, :controller => 'hearthstone', :action => 'show', :id => card, :remote => true, "data-type" => "script" %></th>
    <th><%= card.types %></th>
    <th><%= card.cost %></th>
  </tr>
<% end %>
